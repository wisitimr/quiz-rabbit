# ============================================================
# Quiz Rabbit - Environment Variables
# ============================================================
# คัดลอกไฟล์นี้เป็น .env.local แล้วใส่ค่าจริง
# cp .env.example .env.local
# ============================================================

# --- Database ---
# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:password@localhost:5432/quiz_rabbit

# --- LINE LIFF ---
# LIFF ID (สร้างได้ที่ LINE Developers Console)
# ใช้ฝั่ง client สำหรับ liff.init()
NEXT_PUBLIC_LIFF_ID=1234567890-abcdefgh

# LINE Login Channel ID (ใช้ verify LIFF token ฝั่ง server)
LINE_CHANNEL_ID=1234567890

# LINE Login Channel Secret (ถ้าต้อง verify เพิ่มเติม)
LINE_CHANNEL_SECRET=your_channel_secret_here
